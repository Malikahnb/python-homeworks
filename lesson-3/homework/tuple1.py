# Count Occurrences: Given a tuple and an element, find how many times the element appears in the tuple.

t = (1, 2, 3, 2, 4, 2, 5)  # Example tuple
el = 2  # Element to count

count = t.count(el)  # Using .count() method

print(f"The element {el} appears {count} times in the tuple.")
